spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
  rabbitmq:
    username: alex
    password: password
  cloud:
    stream:
      rabbit:
        binder:
          nodes: localhost:5672
  jpa:
    show-sql: true
